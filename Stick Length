#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;cin>>n;vector<int>d(n,0);
    for(int i=0;i<n;i++){
cin>>d[i];
    }
    sort(d.begin(),d.end());
    int median=d[n/2];long long ans=0;
 for(int i=0;i<n;i++){
    ans+=abs(d[i]-median);
 }
 cout<<ans;
    return 0;
}
