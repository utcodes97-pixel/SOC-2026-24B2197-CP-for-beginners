#include<bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;

    long long n, k, sqrf;

    while(t--) {
        cin >> n;
long long l=-1;long long r=1e9;long long mid;
while(l+1<r){
mid=(l+r)/2;
if((mid*mid)>=n){r=mid;}
else{l=mid;}
}
        cout << r-1 << endl;
    }
}
    
