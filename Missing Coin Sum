#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
   int n;cin>>n;
   vector<int>a(n,0);
   for(int i=0;i<n;i++){cin>>a[i];}  sort(a.begin(),a.end());
   long long x=0;
   for(auto c:a){
      if(x+1<c){cout<<x+1;return 0;}
      else{x+=c;}
   }
   cout<<x+1;
     return 0;
}
