#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
unordered_map<int,int>mp;int n,x;cin>>n>>x;
   mp.reserve(n * 2);
    mp.max_load_factor(0.7);
for(int i=1;i<=n;i++){
int val;cin>>val;
int need=x-val;
if(mp.find(need)!=mp.end()){
cout<<mp[need]<<" "<<i;return 0;
}
mp[val]=i;
}
cout<<"IMPOSSIBLE";
    return 0;
}
